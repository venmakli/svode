[workspace]
members = ["apps/desktop/src-tauri", "crates/svode-cli", "crates/svode-core", "crates/svode-lfs", "crates/svode-mcp"]
resolver = "3"

[profile.lfs-release]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
strip = true
