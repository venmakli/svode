[workspace]
members = ["apps/desktop/src-tauri", "crates/svode-core", "crates/svode-lfs"]
resolver = "3"

[profile.lfs-release]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
strip = true
