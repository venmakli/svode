import type {
  CloneProgress,
  GitAvailability,
  GitStatus,
  SyncResult,
} from "@/types/git";

export type GitAvailabilityDto = GitAvailability;
export type GitStatusDto = GitStatus;
export type SyncResultDto = SyncResult;
export type CloneProgressDto = CloneProgress;
