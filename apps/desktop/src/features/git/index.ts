export type {
  CloneProgress,
  FileGitState,
  FileGitStatus,
  GitAvailability,
  GitCloneProgress,
  GitStatus,
  GitUnpushedCommit,
  SyncResult,
} from "./model/types";
export { setSpaceCloneProgress } from "./api/git-clone-progress-actions";
